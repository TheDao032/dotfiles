# Work git identity — loaded automatically by ~/.gitconfig's includeIf
# block when the active repo is under ~/Documents/Bosch/.
# Source: home/.chezmoidata.toml → [work].
[user]
    name  = {{ .work.name }}
    email = {{ .work.email }}

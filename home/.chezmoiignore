# Files / dirs to ignore when chezmoi reads the source.
# https://www.chezmoi.io/reference/special-files-and-directories/chezmoiignore/

README.md
docs/
LICENSE

# Skip macOS Finder cruft
.DS_Store

# Skip OS-specific files when not on that OS — chezmoi templates these out.
# .Brewfile now installs on Linux too via linuxbrew; macOS-only entries are
# gated inside dot_Brewfile.tmpl. So no OS exclusion needed for it anymore.
{{- if ne .chezmoi.os "linux" }}
# (placeholder for linux-only files when they exist)
{{- end }}
